# Azazel-Gadget Python runtime dependencies (pip-installable).
#
# System packages are still provisioned by installer/stages/10_dependencies.sh
# (apt: python3-yaml, python3-requests, ...). This manifest makes the
# pip-installable deps declarative and reviewable, and is the home for the
# shared azazel-fabric pin.
#
# azazel-fabric provides the shared status view-model (azazel_fabric.view).
# Pinned to an exact tag per Azazel-Fabric design-principles.md §6 (tag
# pinning, not floating version ranges, for the shared-contracts package).
# The code imports it optionally (see py/azazel_gadget/common_view.py), so
# Gadget runs with or without it installed; installing this manifest
# activates the shared view-model.
azazel-fabric @ git+https://github.com/01rabbit/Azazel-Fabric.git@v0.4.0

requests>=2.32,<3
PyYAML>=6,<7
